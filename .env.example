# ============================================================================
# Variables de Entorno - Sistema de Seguimiento de Alumnos
# ============================================================================
#
# Instrucciones:
# 1. Copiar este archivo como .env
# 2. Completar los valores según tu entorno
# 3. NO subir el archivo .env a Git (ya está en .gitignore)
#
# ============================================================================

# ============================================================================
# Base de Datos
# ============================================================================

# Ruta a la base de datos SQLite (desarrollo local)
# En Vercel, esto se sobrescribe automáticamente a /tmp/database.db
DATABASE_PATH=./database.db

# Para migración futura a PostgreSQL (comentado por ahora)
# DATABASE_URL=postgresql://user:password@localhost:5432/seguimiento_alumnos

# ============================================================================
# API
# ============================================================================

# Entorno de ejecución
ENVIRONMENT=development  # development | production

# Puerto para desarrollo local
PORT=8000

# Host para desarrollo local
HOST=0.0.0.0

# ============================================================================
# Seguridad (para futuras versiones con autenticación)
# ============================================================================

# Clave secreta para JWT (generar con: openssl rand -hex 32)
# SECRET_KEY=tu-clave-secreta-muy-segura-aqui

# Algoritmo de encriptación para JWT
# JWT_ALGORITHM=HS256

# Tiempo de expiración del token (en minutos)
# JWT_EXPIRATION_MINUTES=30

# ============================================================================
# CORS
# ============================================================================

# Orígenes permitidos para CORS (separados por coma)
# En desarrollo: *
# En producción: https://tu-frontend.com,https://otro-dominio.com
CORS_ORIGINS=*

# ============================================================================
# Logging
# ============================================================================

# Nivel de logging
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# ============================================================================
# Vercel (solo para despliegue en Vercel)
# ============================================================================

# Estas variables se configuran automáticamente en Vercel
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=tu-app.vercel.app

# ============================================================================
# Notas
# ============================================================================
#
# Para desarrollo local:
# - Usar SQLite con DATABASE_PATH=./database.db
# - CORS_ORIGINS=* (permite todos los orígenes)
#
# Para producción en Vercel (con SQLite):
# - DATABASE_PATH se sobrescribe a /tmp/database.db automáticamente
# - Configurar CORS_ORIGINS con dominios específicos
#
# Para producción con PostgreSQL:
# - Configurar DATABASE_URL con la conexión a PostgreSQL
# - Actualizar repositorios para usar PostgreSQL en lugar de SQLite
# - Configurar variables de entorno en Vercel Dashboard
#
# ============================================================================
